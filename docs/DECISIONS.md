# DECISIONS.md — NetShield

> Append-only. One line per decision: what was chosen, over what, why, and which package settled it. A later session treats every line here as already settled and does not relitigate it.

## Format

`chose X over Y because Z (WP-N.N, YYYY-MM-DD)`

## Pre-build decisions

These were settled while writing the steering documents, before WP-0.1.

- Chose a modular monolith over microservices because a single administrator deploying to one on-premises host needs one process tree to reason about, and module isolation is enforceable by architecture test without a network boundary (pre-build, 2026-09-03)
- Chose PostgreSQL with TimescaleDB over a separate time-series database because one store means one backup, one connection pool, and one query language across inventory joins and telemetry, which matters more at 500 devices than the throughput a dedicated TSDB would add (pre-build, 2026-09-03)
- Chose to split push ingest into .NET and pull collection into Python over a single collector runtime because syslog and flow reception is socket and back-pressure work while SNMP and network CLI is protocol-library work, and each ecosystem is clearly stronger at one of them (pre-build, 2026-09-03)
- Chose read-only device access with no write path of any kind for V1 over including config push because blast radius on production network kit is the one mistake that cannot be rolled back from a browser (pre-build, 2026-09-03)
- Chose relational adjacency tables over a graph database because V1 needs one-hop topology and downstream-reachability suppression, not multi-hop traversal, and a graph store is a Phase-2 architecture change if correlation is ever built (pre-build, 2026-09-03)
- Chose to defer all automation, threat intelligence, correlation-graph, and path-analysis work over including a thin version of each because a thin version of a correlation engine is worse than none and would set the architecture before the data model is proven (pre-build, 2026-09-03)
- Chose file-import-only vulnerability data over active scanning or NVD lookups because the platform must run with no outbound internet dependency inside a customer network (pre-build, 2026-09-03)
- Chose self-hosted Inter woff2 over a font CDN for the same reason (pre-build, 2026-09-03)
- Chose the reference dashboard screenshot as the binding visual source of truth over an independently authored design system because the design direction is already settled and re-deriving it invites drift (pre-build, 2026-09-03)

## Build decisions

- Chose the classic `.sln` format over the .NET 10 default `.slnx` because `CONVENTIONS.md` §1 names `NetShield.sln` in the binding repository layout (WP-0.1, 2026-09-03)
- Chose FluentAssertions 7.2.0 over 8.x because v7 is the last Apache-2.0 release and v8 onwards requires a paid licence for commercial use, which this project has not confirmed (WP-0.1, 2026-09-03)
- Chose to keep the `Aspire.AppHost.Sdk` 13.5.3 version literal in `NetShield.AppHost.csproj` over moving it into `Directory.Packages.props` because it is an MSBuild SDK reference rather than a `PackageReference`, and central package management does not govern SDK imports (WP-0.1, 2026-09-03)
- Chose `xunit.v3` over the SDK template's xUnit v2 because the test projects were still empty and the migration cost only ever grows (WP-0.1, 2026-09-03)
- Chose to set `TargetFramework` in `Directory.Build.props` and delete it from every `.csproj` over leaving the template's per-project declarations because `ARCHITECTURE.md` §10 admits one version floor and a project cannot drift from a value it does not state (WP-0.1, 2026-09-03)
- Chose `insert_final_newline = true` over the `dotnet new editorconfig` default of `false` because the repository will also hold TypeScript and Python, where a trailing newline is the universal convention (WP-0.1, 2026-09-03)
- Chose to enforce this package's own "Done when" criteria as tests in `NetShield.ArchitectureTests` over accepting the zero-tests allowance in `WORK_PACKAGES.md` because `CONVENTIONS.md` §7 requires tests and the invariants have to hold for every project added after this one, not just the nineteen created here (WP-0.1, 2026-09-03)
- Chose to check those invariants by parsing the `.csproj` files on disk over inspecting loaded assemblies because MSBuild-level rules are not observable from compiled metadata and the file-based check covers projects nothing references yet (WP-0.1, 2026-09-03)
- Chose to delete the Worker and Hello World placeholders the templates generate over keeping them because WP-0.1 produces an empty skeleton and a stub that logs on a timer is invented behaviour a later package would have to unpick (WP-0.1, 2026-09-03)
- Chose MailPit `v1.31.0` over the MailHog named in the WP-0.2 entry because MailHog was archived upstream in 2024 at v1.0.1 while MailPit is the maintained drop-in for the same SMTP-sink role, and MailPit's `/readyz` endpoint lets the Aspire dashboard report the resource healthy rather than merely running (WP-0.2, 2026-09-03)
- Chose `timescale/timescaledb:2.29.0-pg17` over `timescale/timescaledb-ha` because the Alpine image is the lighter development container and V1 is explicitly single-node with no HA in scope per `SPEC.md` §3 (WP-0.2, 2026-09-03)
- Chose to set `TIMESCALEDB_TELEMETRY=off` on the PostgreSQL container over accepting the image default because the image reports usage to Timescale on a schedule and `ARCHITECTURE.md` §1 permits no outbound internet call at runtime (WP-0.2, 2026-09-03)
- Chose to let the `netshield` database inherit the `timescaledb` extension from `template1` over running an explicit creation script because the image's own init script installs the extension into `template1` before Aspire creates any database, and a second creation path would only be able to diverge from it (WP-0.2, 2026-09-03)
- Chose `/health` for liveness and `/health/ready` for readiness over the Aspire template's `/health` and `/alive` because the WP-0.2 "Done when" names both paths, and the split keeps an unreachable PostgreSQL from being read as a reason to restart the process (WP-0.2, 2026-09-03)
- Chose to register PostgreSQL and Redis client integrations in `Web.Host` in this package over deferring them to WP-0.3 because `/health/ready` has nothing to report on without them, and a readiness endpoint that only reports on itself is not a readiness endpoint (WP-0.2, 2026-09-03)
- Chose to register `NetShield.Ingest` in the AppHost with `AddServiceDefaults()` alone over leaving it out until Phase 4 because an AppHost that models four of the five runtime processes is a misleading picture of the system, and wiring it to stores it does not yet use would pull Phase 4 work forward (WP-0.2, 2026-09-03)
- Chose to keep every WP-0.2 test container-free over automating the "Done when" criteria against a live stack because `dotnet test` should stay deterministic and runnable without a Docker daemon; container-backed tests arrive as a separate suite when `CONVENTIONS.md` §7's Testcontainers harness lands in WP-0.3 (WP-0.2, 2026-09-03)
- Chose to enforce "no hardcoded connection string" as two repository-wide architecture tests — a scan of every `appsettings*.json` for a `ConnectionStrings` section and a scan of every `src` string literal for a host, credential or store URI — over trusting review because the rule in `SPEC.md` §5 has to hold for every package that follows, not just this one (WP-0.2, 2026-09-03)
- Chose to reject a `limit` above 200 with a 400 over clamping it silently because a caller who asks for 5,000 rows and receives 200 reads the response as "there were only 200", which is a data-loss bug wearing a pagination costume (WP-0.3, 2026-09-03)
- Chose to apply secret redaction by replacing `ILoggerFactory` over decorating each `ILoggerProvider` because a provider-level decorator covers only the providers registered when it ran and silently stops covering the next one somebody adds, which is the failure mode `ARCHITECTURE.md` §8 exists to rule out (WP-0.3, 2026-09-03)
- Chose to forward a log line unmodified when it holds no secret over always rebuilding its state because an untouched line then keeps its alignment and format specifiers and its values keep their types for structured sinks, and only a line that actually carried a secret pays the cost (WP-0.3, 2026-09-03)
- Chose to redact log state, scopes and the rendered message but not the attached `Exception` because rewriting an exception would alter the stack trace an operator needs, and a credential inside an exception message is a bug at the throw site rather than something a sink can repair (WP-0.3, 2026-09-03)
- Chose to resolve an outbox row's event type through an explicit `IntegrationEventRegistry` over `Type.GetType(string)` because a stored string must never be able to choose which type gets constructed, and an event nothing can resolve then fails at the publish rather than writing a row that could only ever fail (WP-0.3, 2026-09-03)
- Chose to leave a repeatedly failing outbox row parked and unprocessed after `MaxAttempts` over deleting it or marking it delivered because a poison message has to stay visible to an operator, and the dispatcher skipping it costs one indexed predicate (WP-0.3, 2026-09-03)
- Chose to keep delivered outbox rows over deleting them on delivery because they are the record of what the system told itself, and pruning belongs to the retention policy rather than to the dispatcher's hot path (WP-0.3, 2026-09-03)
- Chose exponential backoff from the poll interval to `MaxPollInterval`, logging `Error` on the first failure of a run and `Warning` on the ones that follow, over an error per poll because an unreachable database would otherwise produce one `Error` a second for as long as the outage lasted (WP-0.3, 2026-09-03)
- Chose to make `AddOutboxDispatcher` a separate opt-in call over registering the dispatcher inside `AddNetShieldPlatform` because the outbox claims rows without `FOR UPDATE SKIP LOCKED` — correct for the single-node V1 in `SPEC.md` §3 — so exactly one process may dispatch, and that has to be a deliberate choice at the composition root (WP-0.3, 2026-09-03)
- Chose `EFCore.NamingConventions` for `snake_case` over naming every column by hand in each EF configuration because `CONVENTIONS.md` §3 applies to every table in the system and a convention that must be remembered per property is one that will eventually be forgotten on one (WP-0.3, 2026-09-03)
- Chose to register the exception handler in every environment over letting the developer exception page render in Development because `SPEC.md` §5 carves out no environment for a stack trace, and a response shape that differs between Development and Production is one nobody tests (WP-0.3, 2026-09-03)
- Chose not to place the caught exception on the `ProblemDetailsContext` over passing it for a customization to read because anything reachable there could be serialised, and a 500 must carry no message, no type name and no stack trace (WP-0.3, 2026-09-03)
- Chose to replace `Aspire.Npgsql` with `Aspire.Npgsql.EntityFrameworkCore.PostgreSQL` in `Web.Host` over registering both because the outbox needs `PlatformDbContext` and keeping the raw data source alongside it would contribute a second, redundant PostgreSQL readiness check (WP-0.3, 2026-09-03)
- Chose to enforce the `ARCHITECTURE.md` §4 module rules over the `.csproj` reference graph over a reflection-based rule library because a reference an empty project declares is elided from assembly metadata, so an assembly-level rule would pass over all ten still-empty modules and report confidence it does not have; a rule library earns its place in WP-1.1 (WP-0.3, 2026-09-03)
- Chose to enable `CentralPackageTransitivePinningEnabled` and lift `Microsoft.Extensions.Hosting` to 10.0.11 over letting each project resolve its own transitive EF Core version because the provider, the naming convention and the design-time tooling each wanted a different patch of `Microsoft.EntityFrameworkCore.Relational` and the build was picking whichever one a given project saw first (WP-0.3, 2026-09-03)
- Chose to mark `**/Persistence/Migrations/*.cs` as generated code in `.editorconfig` over reformatting each migration by hand because `dotnet ef migrations add` writes a BOM and its own layout, `CONVENTIONS.md` §3 forbids editing a migration after it merges, and otherwise every generated migration would fail the style gate on the day it was created (WP-0.3, 2026-09-03)
- Chose to give each integration test its own database on one shared container over sharing a database across the class because every one of these tests writes to the same outbox table, and a test that owns its database cannot be affected by the order the others ran in (WP-0.3, 2026-09-03)
- Chose `Konscious.Security.Cryptography.Argon2` 1.3.1 over `Isopoh.Cryptography.Argon2` because .NET ships no Argon2 at all, both are MIT, and Konscious exposes the raw parameters NetShield needs to encode into the stored hash rather than a fixed format of its own (WP-0.4, 2026-09-03)
- Chose to store every password as a PHC string — `$argon2id$v=19$m=,t=,p=$salt$digest` — over storing the digest alone because a column holding only the digest can never be re-costed without invalidating every password in it, and the parameters travelling with the hash are what make rehash-on-sign-in possible (WP-0.4, 2026-09-03)
- Chose the OWASP Argon2id parameters `m=19456 KiB, t=2, p=1` with a 128-bit salt as the configurable default over a heavier fixed cost because they are the published floor for this variant and an operator on faster hardware can raise them without a code change or a lockout (WP-0.4, 2026-09-03)
- Chose to answer an unknown username, a wrong password, a disabled account and a locked-out account with one identical 401 over distinguishing them because WP-0.4 requires no user enumeration, and to record the distinction in the log where an operator can see it and a caller cannot (WP-0.4, 2026-09-03)
- Chose to verify a presented password against a decoy hash when the username matches no account over returning as soon as the lookup misses because a 401 that arrives tens of milliseconds early enumerates the user table just as surely as a different error message would (WP-0.4, 2026-09-03)
- Chose two cookies — a short-lived session cookie on `/` and a refresh cookie scoped by `Path` to `/api/v1/auth/refresh` — over one long-lived cookie because the credential presented on every request should be the one worth the least, and the browser then never sends the refresh token to any other endpoint (WP-0.4, 2026-09-03)
- Chose `SlidingExpiration = false` on the session cookie over letting it slide because the refresh token is what extends a session and it rotates, whereas a sliding cookie would extend one indefinitely with nothing to detect a theft by (WP-0.4, 2026-09-03)
- Chose to fix `HttpOnly`, `Secure` and `SameSite=Lax` in code with no configuration binding over making them settable because ARCHITECTURE.md §8 requires all three, and a security property an environment file can turn off is one that will eventually be off in production (WP-0.4, 2026-09-03)
- Chose to revoke the entire refresh-token chain when a already-spent token is presented over revoking only that token because replaying a spent token is the signature of a stolen cookie, and signing out the legitimate holder too is the intended outcome when one of the two holders is an attacker (WP-0.4, 2026-09-03)
- Chose to store only the SHA-256 digest of a refresh token, unsalted, over an Argon2id hash because the input is 256 bits this process generated rather than something a person chose, so there is no candidate list to run against it and no reason to pay a memory-hard cost on every refresh (WP-0.4, 2026-09-03)
- Chose to clear the failure counter when a lockout is applied over leaving it at the maximum because a lapsed lockout should return the account to a full allowance rather than locking again on the next single mistake (WP-0.4, 2026-09-03)
- Chose to answer a wrong *current* password on the change-password endpoint with 422 over 401 because the session is perfectly valid, and WP-0.7 makes a 401 anywhere redirect to sign-in — so a typo would otherwise sign the user out (WP-0.4, 2026-09-03)
- Chose to revoke every other session on a password change and re-issue the caller's own over leaving the other sessions signed in because a password change is what someone does after suspecting their password is known (WP-0.4, 2026-09-03)
- Chose to source the first-run administrator's password from configuration, supplied by a persisted Aspire secret parameter, over shipping a default or generating one and printing it because a shipped default is a shipped vulnerability and SPEC.md §5 does not allow the log to be where a credential is read from (WP-0.4, 2026-09-03)
- Chose to seed only when the `users` table is empty, and to refuse a seed password the policy rejects, over seeding by username or exempting the seed from the policy because an installation whose administrator was deleted has a recovery problem rather than a seeding one, and the one account that administers the system must not be the one account exempt from its rules (WP-0.4, 2026-09-03)
- Chose to give `IdentityDbContext` its own migration history table `__ef_migrations_history_identity` over sharing EF's default with `PlatformDbContext` because independently migrated contexts should each be able to say what they have applied without reading rows written by the other (WP-0.4, 2026-09-03)
- Chose to store `UserRole` as its name in the database and serialise it as its name on the wire over its ordinal because adding a role in WP-0.5 would otherwise renumber every row and every stored response that already meant something else (WP-0.4, 2026-09-03)
- Chose to keep `SessionGrant` out of `IdentitySerializerContext` over marking its members ignorable because an endpoint that returned it by mistake then fails on the first request rather than shipping a refresh token in a response body (WP-0.4, 2026-09-03)
- Chose to hold cookies by hand in the integration-test client over using `CookieContainer` because a `CookieContainer` will not replay a `Secure` cookie over the test host's plain HTTP, and the raw `Set-Cookie` header is where `HttpOnly`, `Secure` and `SameSite` can actually be asserted (WP-0.4, 2026-09-03)
- Chose to replace the cookie handler's redirect events with problem-details responses over accepting its `302` to a sign-in page because CONVENTIONS.md §4 gives the API one error shape and its only client reads status codes (WP-0.4, 2026-09-03)
- Chose to resolve a permission from the session's role on every request over minting permission claims into the cookie because a claim the client holds is a claim the client can forge, and a change to what a role may do then takes effect for sessions that are already open (WP-0.5, 2026-09-03)
- Chose a coarse permission set — one member per SPEC.md §2 area, with a read/write split only where the areas already have one — over a finer grain because a finer grain before the modules exist is guesswork, and adding a member later costs nothing while splitting one that a role map and a dozen endpoints already name costs a migration of intent (WP-0.5, 2026-09-03)
- Chose to keep `CredentialsManage`, `SystemAdminister` and `AuditRead` with the Administrator alone over granting any of them to the Operator because credential lifecycle is the highest-blast-radius privilege in the system and least privilege separates routine operation from credential administration (WP-0.5, 2026-09-03)
- Chose deny-by-default — an ASP.NET Core fallback policy over every endpoint that declares no authorization — over gating only the endpoints that ask for it because a route added without a policy then fails closed as a 401 in development rather than opening quietly in production, and the health endpoints say `AllowAnonymous()` in the diff where a reviewer can see them (WP-0.5, 2026-09-03)
- Chose to materialise a permission policy on demand through an `IAuthorizationPolicyProvider` over registering one policy per permission at startup because adding a member to `Permission` then needs no second edit somewhere a reviewer has to remember to look (WP-0.5, 2026-09-03)
- Chose to carry `must_change_password` as a session claim over reading the `users` table in the authorization handler because the requirement lives in `NetShield.Platform` and ARCHITECTURE.md §4 does not let it reach into a module; the claim is re-minted on sign-in, on refresh and on the change that clears it (WP-0.5, 2026-09-03)
- Chose to exempt exactly three routes from that requirement — `/auth/password`, `/auth/me` and the anonymous `/auth/logout` — over exempting a whole route group because a user who owes a password change has to be able to see who they are, change it, and sign out, and nothing else (WP-0.5, 2026-09-03)
- Chose to record the audit row in middleware after the endpoint over appending it inside each handler's transaction because middleware also records the calls the endpoint never saw — a 401 from authentication, a 403 from authorization — which is the half of an audit log that matters most when something has gone wrong; the cost is a process death between the domain commit and the audit insert losing the row, and the answer if that ever stops being acceptable is the transactional outbox, not a hand-written append in every handler (WP-0.5, 2026-09-03)
- Chose to audit only state-changing methods over recording every request because at the scale in SPEC.md §1 a row per query would bury the rows that describe a change, and SPEC.md §5 asks for the state-changing calls (WP-0.5, 2026-09-03)
- Chose not to audit a request that matched no endpoint over recording every 404 because a 404 for a path nothing serves is a scanner rather than an act, and an audit log full of them is one nobody reads (WP-0.5, 2026-09-03)
- Chose a `BEFORE UPDATE OR DELETE OR TRUNCATE ... FOR EACH STATEMENT` trigger that raises an exception over a PostgreSQL rewrite rule with `DO INSTEAD NOTHING` because the WP-0.5 criterion is that `UPDATE audit_log` *fails*, and a rule that discards the write silently would satisfy the letter of "append-only" while telling the person who tried nothing at all (WP-0.5, 2026-09-03)
- Chose `FOR EACH STATEMENT` over `FOR EACH ROW`, and covered `TRUNCATE` in the same trigger, because a row-level trigger never fires for a statement that matches no rows and never fires for `TRUNCATE` at all — two holes in a rule that is supposed to have none (WP-0.5, 2026-09-03)
- Chose to give `PlatformDbContext` no `DbSet<AuditEntry>` and reach the table with `Set<AuditEntry>()` over exposing one for symmetry with the outbox because a `DbSet` is a handle with `Remove`, `RemoveRange`, `ExecuteDelete` and `ExecuteUpdate` hanging off it, and the rule is that no such path exists (WP-0.5, 2026-09-03)
- Chose to omit `updated_at` from `audit_log` over carrying it for consistency with the CONVENTIONS.md §3 "every table" rule because a column that can never change on a table that can never be updated is a statement the schema cannot keep (WP-0.5, 2026-09-03)
- Chose to enforce the no-update-no-delete rule by scanning the repository's comment-stripped source over reflecting on the built assembly because WP-0.3 already settled that these tests read the repository as files, and a file-level rule also catches a method nobody has called yet — which is the state a mistake is in when it is cheapest to catch (WP-0.5, 2026-09-03)
- Chose to name audit snapshot members after the fact that changed rather than the credential it changed about — `changeRequired`, not `mustChangePassword` — because `SecretRedactor` blanks by property name and does not stop to consider that a boolean cannot be a password, so the honest-looking name would have stored `[REDACTED]` and said nothing (WP-0.5, 2026-09-03)
- Chose to record a failed sign-in against a username nobody holds with no actor and no target over storing the username presented because the username field is where a password gets typed by mistake, and an append-only table is the wrong place for one to land (WP-0.5, 2026-09-03)
- Chose to keep `audit_log` a plain relational table over making it a Timescale hypertable because it is written once per state-changing call rather than thousands of times a second, it is read by actor and by target more often than by time window, and ARCHITECTURE.md §3 reserves hypertables for metrics, flows and log events (WP-0.5, 2026-09-03)
- Chose to move `AddAuthorization` out of the Identity module and into `AddNetShieldAuthorization` in Platform over leaving each module to register what it needs because RBAC is one policy for the whole system, and a host that forgets the call now fails loudly at `UseAuthorization` rather than quietly running with whichever module asked first (WP-0.5, 2026-09-03)
- Chose Tailwind 4 with the DESIGN.md §3–4 tokens in `tailwind.config.ts` loaded by `@config` over Tailwind 4's CSS-first `@theme` or staying on Tailwind 3 because DESIGN.md §2 names `tailwind.config.ts` as where the tokens become the theme and that document is binding, while Tailwind 3 is the superseded major (WP-0.6, 2026-09-03)
- Chose to hold every colour value once in `src/styles/theme.css` as a CSS custom property, with `tailwind.config.ts` naming the tokens that read them, over writing the hexes into the config because the header's theme toggle then swaps a palette without a single component knowing which theme is on, and a component still never sees a hex (DESIGN.md §9.3) (WP-0.6, 2026-09-03)
- Chose to derive the light palette by moving only the surface and text roles and leaving every semantic hue untouched over designing one because DESIGN.md §2 says light is derived from these tokens rather than separately designed, and a hue in NetShield encodes state, which does not depend on which way round the chrome is (WP-0.6, 2026-09-03)
- Chose to test the palette against `docs/DESIGN.md` itself — parsing §3 and failing when a value is missing or when one appears that the table does not name — over trusting review because "never introduce a colour outside the token table" is a rule that has to hold for every package that follows, not just this one (WP-0.6, 2026-09-03)
- Chose `openapi-typescript` plus `openapi-fetch` over `@hey-api/openapi-ts` because CONVENTIONS.md §6 requires a query-key factory per feature and a generator that also emits TanStack Query hooks would own the key structure this project has to own (WP-0.6, 2026-09-03)
- Chose to commit `src/NetShield.Web.Host/openapi/v1.json` and generate the client from the committed copy over generating from a running API because a build that needs the API up to produce a client is a build that cannot run in isolation, and the committed document is the artefact a reviewer can read the contract change out of (WP-0.6, 2026-09-03)
- Chose to produce that document from a host in `NetShield.UnitTests` that mirrors the composition root, guarded by an architecture test comparing the two `Map*Endpoints` lists, over `Microsoft.Extensions.ApiDescription.Server` build-time generation because the build-time generator runs `Program.cs`, and `Program.cs` throws without the Aspire connection strings that exist only at run time (WP-0.6, 2026-09-03)
- Chose to declare each authentication endpoint's response shapes with `Produces`/`ProducesProblem` over leaving the document to infer them because every handler returns `IResult` in order to write cookies as well as a body, nothing can be inferred from that, and a client generated from an empty document describes nothing (WP-0.6, 2026-09-03)
- Chose to put `MapNetShieldSpa` and `AddNetShieldApiDocument` in `NetShield.Platform` rather than in `NetShield.Web.Host` because both need to be exercised by a test and ARCHITECTURE.md §4 lets nothing reference the composition root; the decision to serve the SPA and to describe the API still belongs to `Program.cs`, which calls them (WP-0.6, 2026-09-03)
- Chose to give `/api/{**path}` a fallback of its own, anonymous and carrying `[NoAudit]`, over letting the SPA fallback claim it because an API client that asked for a path nothing serves would otherwise receive HTML with a 200, and the fallback has to behave exactly like the unrouted 404 WP-0.5 chose not to audit (WP-0.6, 2026-09-03)
- Chose to serve the SPA's static files before authentication in the pipeline, with both fallback endpoints saying `AllowAnonymous()`, over placing them after because the shell is what renders the sign-in page and WP-0.5's deny-by-default would otherwise make signing in require a session (WP-0.6, 2026-09-03)
- Chose to have Vite write its build straight into `src/NetShield.Web.Host/wwwroot` over building to `dist` and copying because a copy step is a step that can be skipped, and a host serving last week's bundle fails in a way nobody sees (WP-0.6, 2026-09-03)
- Chose to resolve the API client's `fetch` per call rather than capture `globalThis.fetch` when the module loads because the client is created at import time and anything installed afterwards — the request mock the tests run against — would otherwise never be seen by it (WP-0.6, 2026-09-03)
- Chose to set a nav link's rest and active colours through the router's `inactiveProps` and `activeProps` over listing both in one class string because two Tailwind utilities of the same kind have equal specificity, so which one won would depend on the order Tailwind happened to emit them in (WP-0.6, 2026-09-03)
- Chose to make a sidebar section's open state derive from the active route, overridable by the reader, over synchronising it in an effect because state that follows the route does not need an effect to keep up with it and a cascading render is the usual cost of pretending otherwise (WP-0.6, 2026-09-03)
- Chose to make section rows expand rather than navigate, with an index route redirecting `/security`, `/reports` and `/administration` to their first child, over giving each section a landing page because the reference screenshot shows a chevron and no destination, and a typed URL should still arrive somewhere (WP-0.6, 2026-09-03)
- Chose to read `GET /auth/me` in the header's user block over rendering a placeholder name because it is the shortest honest proof that the generated client is wired end to end, and it stops short of the guard, the redirect and the role-aware hiding that are WP-0.7 (WP-0.6, 2026-09-03)
- Chose to leave the notification bell without a count over matching the reference screenshot's `12` because nothing counts notifications until Phase 6 and a fabricated number in the chrome is worse than a gap in it (WP-0.6, 2026-09-03)
- Chose to apply the stored theme from a small inline script in `index.html` before the first paint over letting the React effect do it because a reader who chose light would otherwise see a dark frame on every load, and DESIGN.md §7 has no room for that (WP-0.6, 2026-09-03)
- Chose `axe-core` called directly from a small helper over `vitest-axe` or `jest-axe` because the wrapper adds a matcher and a dependency and nothing else, and the colour-contrast rule has to be turned off deliberately — jsdom loads no stylesheet, so a contrast verdict there would be about jsdom (WP-0.6, 2026-09-03)
- Chose to make `npm test` run the type check, the lint, the format check and the suite over running the suite alone because CONVENTIONS.md §10 names one frontend command and a gate that only some of the checks answer to is not a gate (WP-0.6, 2026-09-03)
- Chose to return the session's resolved permission set on `AuthenticatedUser` over mirroring the role-to-permission table in TypeScript because authorization policy needs one source of truth, and a second copy in the client would drift into showing a control the API then refuses; the list is presentation only — it is not a claim, nothing the client sends back is read, and every protected request re-resolves the same table server-side (WP-0.7, 2026-09-04)
- Chose to order that list by the declaration order of `Permission` over the set's own enumeration order because two responses for the same role have to be byte-identical, and a set makes no promise about order (WP-0.7, 2026-09-04)
- Chose a pathless `_app` layout route owning both the shell and the guard over guarding inside the shell because an unauthenticated visitor should never see a frame of an application they cannot use, and the twenty-seven route files renamed under it keep their URLs unchanged (WP-0.7, 2026-09-04)
- Chose to put `/login` and `/change-password` outside `_app` over inside it because `_app` is what redirects to them, and a screen under that guard would redirect to itself for ever (WP-0.7, 2026-09-04)
- Chose to have the sign-in route read the cached session and never fetch over resolving it like every other route because asking `/auth/me` there would cost an anonymous visitor a refused refresh on each page load and WP-0.5 writes an audit row for every one, filling the log with refresh attempts nobody made; the cost is that a cold visit to `/login` holding a live cookie shows the form rather than redirecting (WP-0.7, 2026-09-04)
- Chose to attempt the silent refresh in one `openapi-fetch` middleware over doing it per call site because every call is equally likely to be the one that finds the fifteen-minute session cookie gone, and a call site that forgot would surface an expiry as a broken screen (WP-0.7, 2026-09-04)
- Chose to single-flight that refresh over letting each 401 start its own because the refresh token rotates on use and WP-0.4 revokes the whole chain when a spent one is replayed — two concurrent refreshes would sign the user out (WP-0.7, 2026-09-04)
- Chose to exclude `/auth/login` and `/auth/refresh` from the retry over treating every 401 alike because a 401 from either means the credential was wrong rather than that a session lapsed, and retrying is a round trip that can only fail, on a loop (WP-0.7, 2026-09-04)
- Chose to have the middleware announce an unrecoverable expiry to a listener the guard installs over navigating from inside it because the middleware runs inside `fetch` and holds no router, and exactly one thing should decide where an expired session goes (WP-0.7, 2026-09-04)
- Chose to act on the first announcement only over acting on each because a navigation is not instant and any request still in flight while it completes announces again, so each announcement would restart the transition that produced the next one (WP-0.7, 2026-09-04)
- Chose to empty the cache before the sign-out navigation rather than after because the sign-in route decides whether to bounce a caller by reading the cached session, and a cache still holding one sent the user straight back to the screen they had just left (WP-0.7, 2026-09-04)
- Chose to make `useSession` return `undefined` below the guard over promising a user because signing out empties the cache while the shell is briefly still mounted, and a hook that promised one would have to suspend with no boundary above it or assert past the gap — both of which turn a one-frame absence into a render loop (WP-0.7, 2026-09-04)
- Chose to sanitise the post-sign-in return path where it is read over relying on the route's `validateSearch` because a TanStack Router route inherits its parent's search parameters and merges its own over them, so a value the child rejected survives as the root parsed it — which left the sign-in page an open redirect until it was checked at the point of use (WP-0.7, 2026-09-04)
- Chose to tell a failed password change apart by the problem's `code` over its status because the wrong-current-password rejection and the policy rejection both answer 422, and only the code says which field the message belongs beside (WP-0.7, 2026-09-04)
- Chose to render the password policy in the server's own words over restating its rules in the client because the policy is configurable and lives on the server, and a second copy would be wrong on the first installation that changed one (WP-0.7, 2026-09-04)
- Chose one identical message for every refused sign-in over distinguishing a locked account from a wrong password because WP-0.4 answers all four cases with the same 401 to prevent user enumeration, and a client that said more would undo that from its own side (WP-0.7, 2026-09-04)
- Chose to hide a navigation entry the session holds no permission for while leaving its route reachable over blocking the route because hiding is presentation and treating a client-side check as the boundary is how one comes to be relied on; the API refuses the call whether or not the link was drawn (WP-0.7, 2026-09-04)
- Chose to drop a navigation section left with no visible children over showing a chevron over nothing because an absence reads as "not for you" and an empty expander reads as a bug (WP-0.7, 2026-09-04)
- Chose to give the not-found page a splat route under `_app` over leaving it on the root because a mistyped address should still leave the reader a sidebar to navigate out with, and an unauthenticated visitor to a path that does not exist should learn nothing about which paths do (WP-0.7, 2026-09-04)
- Chose to assemble the sign-in and change-password screens from the DESIGN.md §3–6 primitives alone over designing them because the reference screenshot has no signed-out screen and DESIGN.md §9 admits no invented visual direction (WP-0.7, 2026-09-04)
- Chose to pass the API address to the Vite resource a second time as `NETSHIELD_API_URL` over renaming the `web-host` resource so Aspire's own variable survives because the resource name is what the dashboard, the health check and every existing test call it, and an explicit variable is visible at the call site where a shell-safe name is a silent requirement (WP-0.7 fix, 2026-09-04)
- Chose to assert in `OrchestrationTests` that the service-discovery name for `web-host` is *not* a shell identifier over only asserting the replacement exists because the second variable is a workaround, and the test that says so is what tells a later author when it can be deleted (WP-0.7 fix, 2026-09-04)
- Chose to give the session guard an `errorComponent` naming the API over letting TanStack Router's default render because a bare "Something went wrong" with a raw parser message satisfies neither DESIGN.md §8 nor the retry CONVENTIONS.md §6 asks for, and the reader can act on "the API is not responding" (WP-0.7 fix, 2026-09-04)
- Chose a `--migrate` argument on `NetShield.Web.Host` over a sixth migration project or a startup hook because `ARCHITECTURE.md` §2 fixes the process model at five and a separate deployable would change it, while migrating on boot has every replica racing to migrate and leaves the API's account holding DDL rights for its whole life — the same artifact run with an argument gives the operational boundary without inventing a process, and an EF migration bundle can later replace what sits behind that step without changing its shape (WP-1.1, 2026-09-04)
- Chose to run the migration step as a generic host rather than a `WebApplication` because a process that binds a socket while it is halfway through changing a schema can answer a request against it, and the step's whole purpose is that it cannot (WP-1.1, 2026-09-04)
- Chose to have the migration step also run the first-run administrator seeder over migrating alone because a migrated database with no account is still a stack nobody can sign in to, and the seeder is already idempotent against a non-empty `users` table (WP-1.1, 2026-09-04)
- Chose `DeviceRole` as Router, Switch, Firewall, AccessPoint, LoadBalancer, Server, Other over a free-text role because config drift in Phase 7 is measured against a per-role golden template and a template cannot key off prose; `Other` covers a role the operator knows and the enum does not name (WP-1.1, 2026-09-04)
- Chose `CriticalityTier` as Critical, High, Medium, Low, and `DeviceEnvironment` as Production, Staging, Development, Lab, over finer vocabularies because both are small conventional scales that cover the domain without over-modelling it, and a member added later costs nothing (WP-1.1, 2026-09-04)
- Chose `DeviceVendor.Unknown` as distinct from `GenericSnmp` over one catch-all member because "nobody has fingerprinted this yet" and "fingerprinted, and it has no CLI features" are different facts that WP-1.5 needs to tell apart (WP-1.1, 2026-09-04)
- Chose to index `hostname` without a unique constraint over treating it as an identifier because DHCP naming, reused defaults, split DNS, differing search suffixes, cloned systems and imperfect discovery all produce real duplicates, and a unique constraint would turn a descriptive attribute into an accidental identity key that stops discovery recording the estate as it is (WP-1.1, 2026-09-04)
- Chose to make `primary_ip_address` unique only among rows with `deleted_at IS NULL` over a table-wide unique index because a removed device must release its address for the replacement being racked, while the row itself has to stay so that telemetry and audit rows naming it still resolve (WP-1.1, 2026-09-04)
- Chose PostgreSQL `inet` for `primary_ip_address` over `text` because it refuses a value that is not an address and normalises the notation, so two spellings of one address cannot both be stored and both look free — accepting that the unique index has to be created by raw SQL in the migration, since EF will not index a property whose CLR type is not `IComparable` and `IPAddress` is not (WP-1.1, 2026-09-04)
- Chose to keep `state` off both request shapes over accepting an operator-supplied value because reachability is something NetShield observes and WP-1.4 owns every transition; a member a caller cannot send is a rule that cannot be forgotten at a call site (WP-1.1, 2026-09-04)
- Chose PUT with whole-resource replacement over PATCH-style merge for device updates because an omitted optional member is then unambiguous, and a merge shape cannot express clearing a value without a null-versus-absent distinction the JSON contract does not carry (WP-1.1, 2026-09-04)
- Chose to add `OutboxEnlistment` to `NetShield.Platform` over reaching the outbox through `IEventBus` from a module because `IEventBus` writes into `PlatformDbContext` and a module keeps its own context — two contexts are two connections, so "the same transaction" would stop being true exactly where it matters; the module maps `outbox_messages` on its own context and excludes it from its own migrations, which is the arrangement WP-0.3 already made the mapping public for (WP-1.1, 2026-09-04)
- Chose to move `ValidationFilter<T>` from `NetShield.Identity` into `NetShield.Platform` over duplicating it because two modules now validate at the boundary and a module may not reference another module (WP-1.1, 2026-09-04)
- Chose to enforce the two remaining `ARCHITECTURE.md` §4 rules as hand-written reflection tests over adding NetArchTest or ArchUnitNET because two rules in a dozen lines of reflection are easier to be sure of than the same two behind a fluent library the reader also has to know; a library is the answer once the rule set grows past that (WP-1.1, 2026-09-04)
- Chose to make `SessionClaims.CreatePrincipal` internal over exempting it from the new boundary rule because it takes the `User` entity and only `SessionService` calls it — the rule found a real leak on its first run (WP-1.1, 2026-09-04)
- Chose to return `totalCount` on the device list over omitting it because a count over `devices` is cheap at the 500-device target scale in `SPEC.md` §1 and the WP-1.7 table needs one; `CursorPage.TotalCount` is optional by design if that ever stops being true (WP-1.1, 2026-09-04)
- Chose to refuse an unrecognised `sort` value with 400 over falling back to the default because a caller who misspells a field and is served a differently ordered page has no way to notice (WP-1.1, 2026-09-04)
